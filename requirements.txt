# Playwright is PINNED. This host is macOS 13 (Darwin 22.6); current Playwright
# refuses to install ("Playwright does not support chromium on mac13").
# 1.49.1 works against the cached chromium-1148. Do not bump on this host.
playwright==1.49.1
pytest>=8.0
